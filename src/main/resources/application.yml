server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user_db?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

#mybatis-plus:
#  mapper-locations: classpath*:mapper/user/*Mapper.xml
mybatis:
  mapper-locations: classpath*:mapper/user/*.xml
  type-aliases-package: com.user.manager.first.project.user.domain.entity